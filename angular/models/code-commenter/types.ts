export type CommentHeadingType = {
    name: string;
    example: string;
};

export type CommentSettings = {
    type: string;
    textContent: string;
    indentLevel: number;
};

export type Comment = {
    comment: string;
};